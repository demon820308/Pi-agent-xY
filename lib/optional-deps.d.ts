// Type declarations for optional dependencies that are lazily loaded
// These modules are NOT bundled — they are loaded at runtime via Function('return import()') pattern
// No module declarations here to avoid webpack resolution errors
